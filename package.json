{
  "name": "blust-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Unit tests and coverage for Blust",
  "scripts": {
    "prepare": "node ./scripts/setup-git-hooks.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "check": "npm run test:coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
